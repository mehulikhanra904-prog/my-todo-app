{
  "name": "mern-todo-app",
  "private": true,
  "version": "1.0.0",
  "description": "Runs the MERN todo app backend and frontend together",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix backend/client run dev -- --host 127.0.0.1 --port 5173"
  }
}
